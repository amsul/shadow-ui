/*jshint node: true*/

'use strict';

module.exports = {
    options: {
        files: ['package.json', 'bower.json', 'shadow.jquery.json'],
        commitFiles: ['package.json', 'bower.json', 'shadow.jquery.json'],
        push: false
    }
}